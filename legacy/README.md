# Legacy Preservation

The previous GreenLight implementation is preserved outside this new folder:

```text
../greenlight-trader
```

Greenlight 2.0 does not import legacy strategy modules. The old repository can
still be used for audit, but V2 starts from a clean systematic allocator
architecture with dynamic ETF discovery and no privileged sector/theme winner.
